// define variables
string = "abcdefghijklmnopqrstuvwxyz"
number = 0123456789
list = [el1, el2]
list += 5

// define drive base
DRIVE_BASE = BC

// turn the motors of the drive base for 100° each

await drive(100°) // await is used to await the completion of the drive() function
drive(100U)

// define wheel radius in cm
WHEEL_RADIUS = 5
// drive 100cm by calculating wheel circumference and then the degree of motor rotation needed
drive(100cm) //

// define a function
func my_func (args) {
    drive()
//    func asdf(){

//    }
}

func other_func (args, args2, **argv){
}

class ABC {
    var a = 5
    func abc () {}
}

func abc () {

}

func empty_func (){

}

// execute another file
exec file.lm3i

sleep(5) // in seconds

if number > 1
{

}
else
{

}